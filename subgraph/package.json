{
  "name": "amity-subgraph",
  "version": "0.1.0",
  "description": "Subgraph indexing Amity escrow project events (ProjectCreated, MilestoneApproved, DisputeRaised, etc.)",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ amity-subgraph",
    "create-local": "graph create --node http://localhost:8020/ amity-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ amity-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 amity-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.71.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
